I"�<p>Trong bài viết này Việt Anh sẽ hướng dẫn các bạn cài Geany và gcc  để soạn thảo và biên dịch chương trình viết bằng ngôn ngữ C trên Linux (cụ thể ở đây là hệ điều hành Ubuntu).</p>

<h2 id="1trước-hết-bạn-phải-tải-và-cài-đặt-geany">1.Trước hết bạn phải tải và cài đặt Geany:</h2>

<p>Mở Terminal (Ctrl + Alt +t) và gõ lần lượt các lệnh sau để cài đặt trình soạn thảo mã nguồn Geany:</p>

<div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt-get update
<span class="nb">sudo </span>apt-get <span class="nb">install </span>geany
</code></pre></div></div>

<h2 id="2tiếp-đó-cài-đặt-gcc-để-biên-dịch-ngôn-ngữ-c">2.Tiếp đó cài đặt gcc để biên dịch ngôn ngữ c:</h2>

<p>Gõ lệnh sau để cài đặt gói build-essential:</p>

<div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt-get <span class="nb">install </span>build-essential
</code></pre></div></div>

<h2 id="3sử-dụng-geany-để-soạn-thảo-mã-nguồn-và-biên-dịch-code-c">3.Sử dụng Geany để soạn thảo mã nguồn và biên dịch code C:</h2>

<p>Tất cả bạn cần làm là ấn F9 để biên dịch chương trình. Và F5 để chạy chương trình nếu biên dịch thành công.</p>
:ET